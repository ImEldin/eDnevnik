<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Units Management</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css?family=Montserrat:400,800');

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: #e3f2fd;
        }

        h1, h2, .card-header {
            font-weight: 800;
        }

        h1 {
            color: #0d6efd;
        }

        .card {
            border-radius: 15px;
            overflow: hidden;
        }

        .btn-outline-primary {
            transition: all 0.2s ease-in-out;
        }

        .btn-outline-primary:hover {
            color: #fff;
            background-color: #0d6efd;
        }

        .btn-success {
            background-color: #28a745;
            border: none;
        }

        .btn-success:hover {
            background-color: #218838;
        }

        .list-group-item {
            transition: background-color 0.2s;
        }

        .list-group-item:hover {
            background-color: #e9ecef;
        }

        .form-control:focus {
            box-shadow: 0 0 5px rgba(40, 167, 69, 0.8);
            border-color: #28a745;
        }

        .text-muted {
            font-size: 0.85rem;
        }

        .card-header {
            background: linear-gradient(135deg, #6c757d, #0d6efd);
            color: #fff;
        }

        .card-header.bg-success {
            background: linear-gradient(135deg, #28a745, #218838);
        }
    </style>
</head>
<body>
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-5">
        <h1 class="text-primary mb-0">Units Management</h1>
        <a href="/admin-dashboard" class="btn btn-danger">
            <i class="bi bi-arrow-left me-2"></i> Back
        </a>
    </div>

    <div class="card shadow-sm mb-5">
        <div class="card-header text-center">
            <h2 class="h5 mb-0">Available Units</h2>
        </div>
        <ul class="list-group list-group-flush" id="unit-list">
            <% if (units && units.length > 0) { %>
                <% units.forEach(unit => { %>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <div>
                            <strong class="text-dark">
                                <i class="bi bi-book-half"></i>
                                <%= unit.unit_name %>
                            </strong>
                            <small class="text-muted">(Students: <%= unit.number_of_students %>)</small>
                        </div>
                        <div class="d-flex">
                            <a href="/admin-dashboard/add-subject/<%=unit.id%>" class="btn btn-outline-primary btn-sm">
                                <i class="bi bi-plus-square"></i> Add Subjects
                            </a>
                        </div>
                    </li>
                <% }) %>
            <% } else { %>
                <li class="list-group-item text-center text-muted">No units available.</li>
            <% } %>
        </ul>
    </div>

    <div class="d-flex justify-content-center mt-4">
        <a href="/admin-dashboard/add-unit" class="btn btn-success btn-lg">
            <i class="bi bi-plus-circle me-2"></i> Add New Unit
        </a>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
